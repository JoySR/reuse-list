const COMMON = {
  TITLE: 'Reusable List',
};

export const COMMON_BASE_URL = 'https://vicia.me/wp-json';
export const WP_BASE_URL = `${COMMON_BASE_URL}/wp/v2`;
export const JWT_TOKEN_URL = `${COMMON_BASE_URL}/jwt-auth/v1/token`;

export default COMMON;
