// Specify your api url
const BASE_URL = '';

const COMMON = {
  TITLE: 'Reusable List',
};

export const USER_BASE_URL = `${BASE_URL}/user`;
export const LIST_BASE_URL = `${BASE_URL}/list`;
export const ITEM_BASE_URL = `${BASE_URL}/item`;

export default COMMON;
